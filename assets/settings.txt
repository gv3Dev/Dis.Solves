let CURRENT_VERSION_OUT = 2.1971;

let SCRIPT_INFO = `Script has better auto answer for quizzes and tests,
now uses the string similarity library and a bunch of text preprocessing 
along with cosine vector calculation to determine the correct answer choice. It also
grabs the best answer from brainly using an algorithm which uses the stars, votes, and thanks.`;

let EDGEPLUS_MSG = `If your purchase was accepted on the payment platform, click the blue EdgePlus link then refresh this page 🙏`;

let DISABLED_USERS = [    
  
    {
        "user_key":"",
        "reason":"",
    }

];
let ACTIVE = true;
let RUN_THIS = `console.log("server_accessed")`;
